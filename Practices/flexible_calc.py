#AP 1st Period, Flexible Calculator

def sum(*numbers):
    for i in numbers:
        total += numbers
    return total
def average(*numbers):
    for i in numbers:
        total += numbers
        amount = 0
        amount +=1
    return total/amount
def max(*numbers):
    for i in numbers:
        max=0
        if i > max:
            max = i
        else:
            continue
    return max
def min(*numbers):
    for i in numbers:
        temp=i
        if i < temp:
            max = i
        else:
            continue
    return max
def product(*numbers):
    for i in numbers:
        total = 1
        total *= numbers

print(max(1,2,3,4,5,6,7,8))